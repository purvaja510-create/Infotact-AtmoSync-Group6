version: 2

sources:
  - name: raw
    description: "Raw IoT sensor readings ingested from Kafka into Snowflake."

    database: ATMOSYNC_DB
    schema: RAW

    tables:
      - name: sensor_readings_raw
        description: "Raw sensor readings generated by the IoT simulator."